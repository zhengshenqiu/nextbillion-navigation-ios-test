#import <Foundation/Foundation.h>

/**
 Posted when something changes in the shared `NBNavigationSettings` object.
 
 The user info dictionary indicates which keys and values changed.
 
 :nodoc:
 */
extern const NSNotificationName NBNavigationSettingsDidChangeNotification;
